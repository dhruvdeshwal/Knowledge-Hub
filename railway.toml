[build]
builder = "NIXPACKS"
buildCommand = "npm ci --legacy-peer-deps && npm run build && cd server && npm ci --omit=dev"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
